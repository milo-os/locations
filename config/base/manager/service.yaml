apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: locations
    app.kubernetes.io/managed-by: kustomize
  name: locations-webhook
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: locations
    app.kubernetes.io/name: locations
